openldap:
  env:
    LDAP_ORGANISATION: "D2iQ"
    LDAP_DOMAIN: "d2iq.lab"
  adminPassword: admin
  configPassword: config
  persistence:
    enabled: true
  phpldapadmin:
    enabled: true
    ingress:
      enabled: true
      ingressClassName:
      path: /
    env:
      PHPLDAPADMIN_LDAP_CLIENT_TLS_REQCERT: "never"
